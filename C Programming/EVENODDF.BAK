#include<stdio.h>
#include<conio.h>
int even_Odd(int);
void main()
{
 int flag=0,n;
 clrscr();
 printf("enter the number you want to check\n");
 scanf("%d",&n);
 flag=even_Odd(n);
  if (flag==0)
  {
   printf("the number %d is Odd",n);
   }
    else{
     printf("the number %d is even",n);
     }
     getch();
    }
     int even_Odd(int n)
     {
      int flag=0;
      if (n%2==0)
      {
       flag=1;
       return flag;
      }
       else{
	flag=0;
	return flag;
	}
       }