#include<stdio.h>
#include<conio.h>
int fact(int);
void main()
{
 int num,temp,result;
 clrscr();
 printf("enter the number\n");
 scanf("%d",&num);
 result= fact(num);  //calling function//
  printf("the factorial of num %d is %d\n",num);
  getch();
  }
   int fact(int num)
   {
    int i,temp=1;
    for(i=1;i<=num;i++)           //definition called function//
    {
     temp=temp*i;
     }
     return temp;
      return num;
   }